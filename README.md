# PrimePlots

Django backend and frontend project for PrimePlots.

## Setup

- Backend: `backend/`
- Frontend: `frontend/`

## Notes

- Backend uses Django
- MySQL connection is configured via `backend/.env`
